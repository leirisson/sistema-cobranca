# Tasks — Onboarding / Configurações (ONB)

- [x] ONB-01 — Modelar entidade `Configuracao` (domínio + Prisma), porta `ConfiguracaoRepository`, migration. Cifragem de `asaasApiKey` via `crypto` nativo (AES-256-GCM), nova env var `CONFIG_ENCRYPTION_KEY`. Registro único via id fixo (`"default"`), não `findFirst()` — ver `claude.md` seção 8.
- [x] ONB-02 — `AsaasGateway` (via `ResolverCredenciaisAsaasUseCase`) e `ConfirmarPagamentoUseCase` (via `resolverConfirmacaoHabilitada`) passam a resolver a credencial/toggle via `ConfiguracaoRepository`, com fallback para env var (`ASAAS_API_KEY`, `CONFIRMACAO_PAGAMENTO_HABILITADA`) quando não configurado pela tela. Divergiu do rascunho original: precisou de um campo extra `confirmacaoPagamentoConfiguradaPeloUsuario` (booleano sem `null` não diferencia "nunca configurado" de "desligado explicitamente") e de mover a instanciação dos use cases para dentro dos handlers HTTP (`cobrancas.ts`, `webhook-asaas.ts`) para resolver a cada requisição, não só no boot.
- [x] ONB-03 — Endpoints `GET /configuracoes` (nunca retorna segredo em claro, só indicador "configurado" + últimos 4 dígitos), `PUT /configuracoes` (atualiza chave Asaas/nome remetente/toggle, edição parcial, `asaasApiKey: ""` remove a credencial). Documentado em `contrato-api/endpoints.md` (API-R-03).
- [x] ONB-04 — Endpoints proxy para a Evolution API: `POST /configuracoes/whatsapp/conectar` (devolve QR Code em base64), `GET /configuracoes/whatsapp/status` (consulta `connectionState` ao vivo). Formato de resposta confirmado contra a instância local real antes de implementar.
- [x] ONB-05 — Templates de mensagem/e-mail/confirmação (`template-mensagem.ts`, `template-email.ts`, `template-confirmacao.ts`) passam a aceitar `nomeRemetente` opcional vindo da `Configuracao`.
- [x] ONB-06 (frontend) — Tela `/configuracoes`: formulário de chave Asaas + nome remetente + toggle de confirmação; bloco de conexão WhatsApp com QR Code e status ao vivo (polling via Server Action a cada 3s).
